## 1. FileProvider Setup

- [x] 1.1 Create `res/xml/file_paths.xml` — external-files-path for `TapLog/` subdirectory
- [x] 1.2 Register `FileProvider` in `AndroidManifest.xml` — authority `ca.taplog.app.fileprovider`, exported false, grant-uri-permissions true

## 2. PDF Generation

- [x] 2.1 Create `data/PdfReportGenerator.kt` — `fun generate(inspection, asset, site, org, deficiencies, context): File`
- [x] 2.2 Implement page layout: header section (TapLog + org name + generation date)
- [x] 2.3 Implement site/client section: site name, address, client name, contact name
- [x] 2.4 Implement asset section: asset name, OFC type label, location, tag ID
- [x] 2.5 Implement inspection section: date, inspector name, cert number, result (with visual emphasis on PASS/FAIL/REQUIRES ATTENTION)
- [x] 2.6 Implement deficiencies section: numbered list of code + description + severity; "No deficiencies recorded" if empty; truncate at 10 with "… and N more" note
- [x] 2.7 Implement footer: next inspection due date + "Generated by TapLog"
- [x] 2.8 Write PDF to `context.getExternalFilesDir("TapLog")/taplog_report_<inspectionId_prefix>.pdf`

## 3. Report Repository

- [x] 3.1 Create `data/ReportRepository.kt` — `suspend fun generateAndGetUri(inspection, asset, site, org, deficiencies, context): Uri`
- [x] 3.2 Call `PdfReportGenerator.generate()`, get `File`
- [x] 3.3 Convert `File` to content URI via `FileProvider.getUriForFile(context, "ca.taplog.app.fileprovider", file)`

## 4. ViewModel and Intent

- [x] 4.1 Add `DeficiencyDao.getByInspection(inspectionId): List<Deficiency>` to `EmberDAO.kt`
- [x] 4.2 Add `getDeficienciesForInspection(inspectionId): List<Deficiency>` to `EmberRepository.kt`
- [x] 4.3 Add `_shareReportEvent: MutableSharedFlow<Intent>` and `shareReportEvent: SharedFlow<Intent>` to `EmberViewModel`
- [x] 4.4 Add `shareReport(inspection: Inspection, asset: Asset)` to `EmberViewModel` — fetches site, org, deficiencies; calls `ReportRepository.generateAndGetUri()`; builds share `Intent`; emits on `shareReportEvent`
- [x] 4.5 Wire `ReportRepository` into `TapLogApplication.kt`

## 5. UI — AssetDetailScreen

- [x] 5.1 Add "Share Report" `TextButton` or icon to each `InspectionHistoryCard` — calls `onShareReport(inspection)`
- [x] 5.2 Add `onShareReport: (Inspection) -> Unit` callback parameter to `AssetDetailScreen`
- [x] 5.3 Wire `onShareReport` in `MainActivity` `AssetFound` handler → calls `viewModel.shareReport(inspection, state.asset)`

## 6. MainActivity — Share Intent Observer

- [x] 6.1 In `EmberScanScreen` (or `MainActivity`), collect `viewModel.shareReportEvent` and call `context.startActivity(Intent.createChooser(intent, "Share Report"))`
